function title() {
  background(0);
  textAlign(CENTER);
  fill(255,255,255);
  textSize(66);
  text("Swift",width/2,height/2)
  textSize(14);
  text("Press SPACEBAR to start", width/2, height-20);
}
